# Сессия 2 · Замер: последовательный прогон

**Дата:** 23–24 августа 2026
**Где:** отдельная чистая сессия Claude Code, папка `hw2-measure-sequential`
**Промпт:** [docs/prompts/sequential-run.md](../docs/prompts/sequential-run.md)
**Модель:** Opus 5, та же, что в основной работе

> Вывод сессии приведён **дословно**, без сокращений и правок.
> Сравнение с веером — отдельным разделом, ниже, после разбора.
> Файл только дописывается.

---

## Вывод сессии (дословно)

Спецификация прочитана. Теперь считаю ожидаемые значения по формулам — в черновике, вне репозитория, с якорением на docs/oracle.md.

Ran 10 commands

Данные посчитаны и заякорены (все 5 точек PMT сошлись копейка в копейку). Пишу файлы.

Ran 11 commands

Готово. Заглушка на месте, набор красный.

1. Время

~16 минут (15 мин 58 с): с 23:59:05 до 00:15:03 MSK.

2. Контекст

Примерно 10–12 % окна. Это самооценка по объёму прочитанного и написанного (spec.md ~37 КБ, oracle.md ~8 КБ, черновые расчёты, ~930 строк кода), а не измерение — счётчика токенов у меня нет.

3. Тесты

Файл	Тестов

tests/core/base.spec.ts	32

tests/core/scenarios.spec.ts	17

Всего	49

Плюс третий файл — tests/core/invariants.ts: общая функция checkInvariants() (INV-01…INV-08 + REQ-26/REQ-27), вызывается в каждом кейсе обоих наборов. Тестов в нём нет — REQ-16 требует именно проверяльщика, а не отдельных тестов; имя файла совпадает с тем, что уже прописано в package.json → lint:forbidden.

4. Прогон против заглушки

npx playwright test

49 failed, 0 passed, 0 skipped. Все 49 падений — с одним и тем же сообщением Error: NotImplemented, то есть каждый тест действительно доходит до вызова ядра, а не падает на импорте или компиляции.

Чем заякорено

Все пять точек PMT из oracle.md сошлись копейка в копейку: O-01 22 244,45 · O-02 27 963,94 · O-03 10 000,00 · O-07 18 556,33 · O-08 48 944,33. Переплату с колонкой PMT не сверял — врезка в oracle.md объясняет, почему там расхождение ожидаемо.

Отдельно проверил транскрипцию: временно подложил в src/core/index.ts сверочную реализацию (черновик, вне репозитория) — 49/49 зелёных, затем побайтово вернул заглушку. Это ловит опечатки в числах, но не ошибку прочтения спецификации: числа и реализация выведены из одного и того же моего разбора.

Кейсы, требующие якоря

REQ-09, NaN / Infinity / null — проверяю toContain('NOT_A_NUMBER'), а не точный список кодов. Спецификация не говорит, даёт ли amount: NaN заодно AMOUNT_NOT_INTEGER и AMOUNT_OUT_OF_RANGE. Точный набор вписывать не стал.

O-04, O-05, O-06 (страховка, reduceTerm, reducePayment) — внешнего якоря нет вовсе, oracle.md фиксирует это прямо. Числа выведены только из формул spec.md; держатся на инвариантах и на перекрёстной сверке трёх сценариев с общими параметрами.

Не написан кейс «досрочка в месяц, когда кредит и так закрывается регулярным платежом»: спецификация не определяет, считать ли её проигнорированной (REQ-25) или списанной в ноль с полным излишком (REQ-23). Правдоподобное число здесь было бы хуже отсутствия теста.

Число месяцев после reduceTerm (47 в O-05) отдельно перепроверено двумя независимыми способами — помесячной симуляцией и замкнутой формулой; сошлись во всех трёх случаях, где это применялось.
