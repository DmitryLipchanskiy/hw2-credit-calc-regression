# Сессия 3 · Агент-скептик

**Дата:** 24 августа 2026
**Где:** отдельная сессия Claude Code, worktree `/Users/qamac/WebstormProjects/wt-skeptic`, ветка `skeptic`
**Промпт:** [docs/prompts/skeptic.md](../docs/prompts/skeptic.md)
**Базовая линия на входе:** 156 passed

> Вывод скептика приведён **дословно**, без сокращений и правок.
> Разбор находок — отдельным разделом, ниже, после его получения.
> Файл только дописывается.

---

## Вывод скептика (дословно)

Отчёт скептика

Базовая линия: 156 passed (7.7s) на нетронутом коде. Все мутации ниже вносились по одной, каждая откатывалась перед следующей.

Всего 23 мутации: 13 поймано, 8 не поймано, 2 эквивалентны (ничего не меняют в выводе — дырой не считаются).

1. Мутации, которые не поймал НИКТО

Каждая проверена на наблюдаемость: показано, что вывод ядра действительно меняется.

🔴 M-02, M-10, M-11 — платёж, пересчитанный при reducePayment, не закреплён ничем

-        payment = computePayment(balance, termMonths - month, i, zeroRate);

+        payment = computePayment(balance, termMonths - month + 1, i, zeroRate);   // M-02

+        payment = computePayment(balance, termMonths - month, i, zeroRate) - 1;   // M-10

+        payment = computePayment(balance, termMonths - month, i, zeroRate) - 100_000; // M-11

156 passed во всех трёх случаях. M-02 — классический off-by-one в числе оставшихся периодов (REQ-22). M-10 — расхождение ровно в копейку, то самое, ради которого взят вариант задания. M-11 — занижение платежа на 1000 ₽ каждый месяц.

Замер последствий M-11 (1 000 000 ₽ / 12 % / 60 мес., досрочка 200 000 ₽ в мес. 12, reducePayment):

	чистый код	M-11	156 тестов

платёж мес. 13–59	16 977,68 ₽	15 977,68 ₽	зелёные

последний платёж	16 977,69 ₽	77 200,29 ₽	зелёные

totalInterest	281 862,05 ₽	295 084,65 ₽	зелёные

Механизм: корректирующий последний платёж (REQ-05) поглощает любую ошибку в платеже, а инварианты после этого сходятся. Все проверки REQ-21/REQ-22 сформулированы как отношения — «строк ровно 60», «платёж 13 < платежа 12», «после 13-го платёж один и тот же». Ни одна не называет числа. Асимметрия полная: противоположный знак (- month - 1, M-03) ловится тремя тестами, потому что срок становится меньше 60; всё, что удлиняет амортизацию, невидимо.

🔴 M-01 — округление страховки

-  return roundHalfUp((balanceBefore * ratePercent) / 100);

+  return Math.floor((balanceBefore * ratePercent) / 100);

156 passed. Наблюдаемо: 1 000 000 ₽ / 12 % / 36 мес. / страховка 1 % — премия 25-го месяца 373 830 → 373 829 коп., totalInsurance 2 079 414 → 2 079 413.

Премия месяца 1 закреплена точным числом (10 000,00 ₽), но при этих параметрах она целая — округление в ней не участвует. Премии 13-го и 25-го месяцев, где дробная часть есть, проверяются только неравенствами <.

🟠 M-22 — округление в ветке нулевой ставки

-    return roundHalfUp(balance / months);

+    return Math.floor(balance / months);

156 passed. Единственный кейс нулевой ставки — O-03 (12 000 000 коп. / 12), делится нацело. На 12 345,67 ₽ / 0 % / 7 мес. платёж 176 367 → 176 366, последний 176 365 → 176 371.

🟠 M-06, M-05, M-04 — границы валидации отвергают валидный вход

- if (!Number.isInteger(month) || month < 1 || (termLimit !== null && month > termLimit))

+ if (!Number.isInteger(month) || month < 1 || (termLimit !== null && month >= termLimit))   // M-06

- if (term < TERM_MIN || term > TERM_MAX) found.add('TERM_OUT_OF_RANGE');

+ if (term <= TERM_MIN || term > TERM_MAX) found.add('TERM_OUT_OF_RANGE');                   // M-05

- } else if (insuranceRate < INSURANCE_RATE_MIN || insuranceRate > INSURANCE_RATE_MAX) {

+ } else if (insuranceRate < INSURANCE_RATE_MIN || insuranceRate >= INSURANCE_RATE_MAX) {    // M-04

156 passed в каждом случае. Что при этом отвергается:

Мутация	Вход, разрешённый спецификацией	Результат

M-06	досрочка в месяц termMonths (REQ-08: 1 … termMonths)	EARLY_MONTH_OUT_OF_RANGE

M-05	termMonths = 1 (REQ-06: 1 … 360)	TERM_OUT_OF_RANGE

M-04	страховка ровно 10,00 % (REQ-07: 0.00 … 10.00)	INSURANCE_RATE_OUT_OF_RANGE

Верхняя граница отвергнутых значений покрыта везде (999 999, 10 000 000 001, 361, 101, 15), нижняя граница принимаемых — нет. Границы суммы (1 000 000 и 10 000 000 000 коп.) закрыты случайно: их использует набор degenerate-кейсов.

2. Пойманные мутации

#	Мутация (суть diff'а)	Упало	Кем поймана

M-16	Math.min(requested, balanceAfter) → balanceBefore	19	REQ-19, REQ-23, REQ-25

M-19	totalPrincipal += … + row.earlyRepaymentExcess	19	REQ-19, REQ-23, REQ-25

M-12	mode === 'reducePayment' → 'reduceTerm'	12	REQ-21, REQ-22, REQ-24, REQ-25

M-23	principal = payment - interest - 100	12	REQ-12 ×4, REQ-13 ×2, REQ-21, REQ-22, degenerate ×4

M-08	досрочка применяется до регулярного платежа (REQ-15, шаги 6↔7)	7	REQ-19, REQ-20 ×4, REQ-23 ×2

M-09	Math.pow(1 + i, months) → months - 1	7	REQ-12 ×4, REQ-26, REQ-21, REQ-22

M-07	computeInsurance всегда возвращает 0	6	REQ-17 ×4, REQ-19, REQ-37

M-03	termMonths - month → - month - 1	3	REQ-22 ×3

M-20	entry.month > actualTermMonths → >=	3	REQ-25 ×3

M-15	validate возвращает только первый код	2	REQ-10 ×2

M-21	база страховки balanceBefore → input.amount	2	REQ-17 ×2

M-13	roundHalfUp(balanceBefore * i) → Math.floor	1	REQ-11 (O-07)

M-18	annualRatePercent === 0 → <= 0.01	1	REQ-13

Две мутации оказались эквивалентными и в счёт не идут: i = rate / 1200 вместо rate / 100 / 12 (вывод байт в байт совпал на шести кейсах, включая длинные сроки и некруглые ставки) и zeroRate = rate < 0.01 — в интервале (0; 0,01) валидных ставок не существует, потому что REQ-06 отвергает больше двух знаков. Оговорка REQ-13 про «условие вида rate < 0.01 — ошибка» нефальсифицируема по построению; ошибкой является только <=.

Отдельно про M-13 и M-18: их держит по одному тесту. Всё правило округления процентов — во всех месяцах всех графиков — висит на base.spec.ts:359, где проверено значение процентов первого месяца одного кейса O-07. Вся граница ветвления REQ-13 — на base.spec.ts:433. Соседние тесты REQ-13 (totalInterest > 0, проценты первого месяца === 100) границу не проверяют: проценты считаются от i независимо от ветки, и при zeroRate они остаются прежними.

3. Слабые тесты

Тавтологии — доказаны мутацией M-23 (principal = payment - interest - 100, поймана 12 тестами; эти три остались зелёными, хотя именно они по названию обязаны были упасть первыми):

Файл	Чем слаб

base.spec.ts:492 REQ-15: the principal of the first month is the payment minus the interest	monthlyPayment в реализации выведен как first.principal + first.interest. Тест проверяет principal === monthlyPayment - interest — тождество, истинное при любой реализации. Ноль информации.

base.spec.ts:532 REQ-26: monthlyPayment equals the total of the first row when there is no insurance	То же тождество плюс INV-04. Обе стороны сдвигаются вместе при любой ошибке в платеже.

base.spec.ts:497 REQ-15: the balance after the first month is the amount minus the principal	Следствие INV-01/INV-03, проверяемых на каждом кейсе.

Тест написан вслепую по спецификации, где monthlyPayment — «первый регулярный платёж», величина независимая. Тождеством его сделал выбор реализации. Это ровно тот случай, ради которого разводили авторов: независимость промпта не спасла, потому что проверка сравнивает два поля одного и того же результата.

Дубли инвариантов (не вредны, но создают видимость покрытия — в счётчике 156 они занимают 7 позиций):

base.spec.ts:139 и :146 — целочисленность полей, уже проверяется checkInvariants на каждом успешном расчёте;

base.spec.ts:205 — balanceAfter === 0 последней строки, это дословно INV-02;

base.spec.ts:521 — actualTermMonths === schedule.length, дословно INV-05;

scenarios.spec.ts:620 — INV-07, комментарий в теле это признаёт;

scenarios.spec.ts:663 the excess is not part of totalPaid — тело переформулирует INV-08; сверх инварианта тест не добавляет ничего, хотя название обещает адресную проверку исключения излишка;

invariants.ts:101 — principal >= 0 уже проверено циклом по ROW_MONEY_FIELDS строкой выше (:91). После ослабления D-23 ассерт стал буквальным дублем.

Проверки без величины: scenarios.spec.ts:355 (insurance > 0), :538 (balanceAfter > 0), base.spec.ts:424, :574. По отдельности законны, но в зоне страховки и досрочки они — единственное, что есть, и вместе с отношениями < дают M-01 пройти.

Characterisation-тесты — где фиксация маскирует дыру:

scenarios.spec.ts:876 reducePayment after an earlier reduceTerm restores the original term (D-22). Написан ровно про ту строку, которую ломает M-02, и выглядит как закрытие неоднозначности REQ-22. Но фиксирует он только число строк (=== 60), а termMonths - month + 1 число строк не меняет. Тест создаёт впечатление, что спорное место закрыто, и при этом пропускает единственную ошибку, которая в нём вероятна.

scenarios.spec.ts:921–952 — четыре degenerate-кейса (100 % / 360 мес.). Они фиксируют principal === 0 во всех месяцах кроме последнего, то есть закрепляют как ожидаемое поведение, при котором тело не гасится 359 месяцев, а весь долг закрывается одним платежом. При этих параметрах платёж ни на что не влияет, поэтому кейсы почти нечувствительны к ошибкам в нём. Полезная работа этих четырёх тестов — покрытие границ диапазонов amount и annualRatePercent, а не проверка расчёта.

4. Требования: покрытие

Не закреплено числом ни одно значение в зоне O-04, O-05, O-06 — там, где oracle.md пуст. Это не упрёк за отсутствие якоря (он честно объявлен), а констатация следствия: всё, что там есть, — отношения между двумя прогонами и инварианты. Обе категории переживают M-01, M-02, M-10, M-11, M-22.

Требование	Состояние

REQ-03	Нет теста, который бы его называл. Косвенно — через Number.isInteger в инвариантах. Запрет «хранить деньги как float между операциями» не проверяется ничем: результат целый, а промежуточное представление снаружи не видно.

REQ-07	Границы 0,00 % и 10,00 % не проверены ни разу (используются только 1 % и невалидные 15 %) → M-04. Требование «не более 2 знаков» для страховки непроверяемо: в таблице REQ-09 нет соответствующего кода. Расхождение внутри самой спецификации.

REQ-06	termMonths = 1 не проверен ни как валидный вход, ни как расчётный кейс → M-05. Кредит на один месяц не считается нигде: ветка «первый месяц он же последний» не исполняется.

REQ-08	month === termMonths не проверен → M-06.

REQ-13	Ветвление держит один тест; округление в ветке нулевой ставки не закреплено → M-22.

REQ-17	Момент начисления закреплён хорошо ([1, 13, 25]). База и округление — только неравенствами → M-01.

REQ-19	Проверено только закрытие через reduceTerm. Комбинации «страховка + reducePayment» нет ни одной.

REQ-21 / REQ-22	Проверены полностью в отношениях, ни одного абсолютного значения после месяца досрочки → M-02, M-10, M-11.

REQ-24	Смешение режимов покрыто. Досрочки, попадающей в страховой месяц, нет.

REQ-36	Держится pre-commit хуком и npm run lint:forbidden, в 156 тестов не входит. grep по tests/** подтверждает: ни одного допуска, ни одного toBeCloseTo — правило соблюдено.

REQ-28 … REQ-35	UI отсутствует в src/ целиком (src/core/ — единственный каталог). Вне 156 по факту.

INV-01 … INV-08	Все восемь реализованы, вызываются на каждом успешном расчёте обоими spec-файлами, и invariants-selfcheck.spec.ts доказывает, что проверяльщик не пустой. Это сделано добротно.

5. Что проверил и не нашёл

Допуски в денежных сравнениях — grep по toBeCloseTo|Math.abs|epsilon|tolerance|delta в tests/**: пусто. REQ-36 в коде проверок соблюдён.

Отключённые проверки — .only, .skip, пустые тела: нет.

Границы суммы и ставки — 1 000 000 и 10 000 000 000 коп., 0 % и 100 %: покрыты (через degenerate-кейсы), мутация на них ловится.

Порядок применения досрочек — перемешанный входной массив сверяется с упорядоченным построчно, дыры нет.

Инвариант-проверяльщик — не декорация: negative control из 11 кейсов, каждый ломает ровно один инвариант.

Мутации, которые я ожидал провести и не смог — смена знака в аннуитетной формуле, игнорирование страховки, страховка от первоначальной суммы, досрочка до начисления процентов, перестановка режимов, излишек в теле, ограничение досрочки не тем остатком, возврат первой ошибки вместо всех, off-by-one в степени: все девять ловятся, причём семь — многими тестами сразу. Зона REQ-15/REQ-20/REQ-23/REQ-25 закрыта плотно.

i = rate / 1200 — не дыра, а эквивалентный мутант: вывод совпал побайтово.

6. Самая опасная находка

Регулярный платёж, пересчитанный после досрочки в режиме reducePayment, не закреплён ни одним числом — любая ошибка в нём, от копейки до тысячи рублей в месяц, целиком поглощается корректирующим последним платежом (REQ-05), и все 156 тестов остаются зелёными.

Рабочая копия возвращена в исходное состояние: git checkout -- src/ выполнен, git status чист, контрольный прогон — 156 passed (6.9s). Каталог dist/ пересобирался для замеров и игнорируется .gitignore; коммитов я не делал.
