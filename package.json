{
  "name": "hw2-credit-calc-regression",
  "version": "1.0.0",
  "private": true,
  "description": "Annuity credit calculator regression suite (TypeScript + Playwright)",
  "scripts": {
    "test": "playwright test",
    "test:core": "playwright test tests/core",
    "build": "tsc -p tsconfig.build.json",
    "oracle:ts": "node scripts/run-cases-ts.mjs",
    "oracle:py": "python oracle/run_cases.py",
    "oracle:compare": "python oracle/compare.py oracle/ts-results.json oracle/expected.json"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
